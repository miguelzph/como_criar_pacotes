# local
pip install . dentro dessa pasta

# pypi tests
python setup.py sdist
pip install twine
twine upload --repository-url https://test.pypi.org/legacy/ dist/*


# installing
pip install --index-url https://test.pypi.org/simple/ distri-probability-tests


# pypi
twine upload dist/*
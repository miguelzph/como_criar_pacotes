from setuptools import setup

setup(name='distri_probability_tests', # nome único
      version='1.1',
      description='Gaussian distributions',
      packages=['distri_probability_tests'], # pasta
      author = 'Miguel',
      author_email = 'name@gmail.com',
      zip_safe=False) # não pode ser zipado
